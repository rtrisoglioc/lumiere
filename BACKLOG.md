# LUMIÈRE v2.0 — BACKLOG (obvious improvements deferred, not built)

- Full ES→EN interface copy migration (doc rule 6 wants English UI; i18n infra retained for now).
- Home dashboard "create" dialog vs `/create` page: unify into a single create entry point.
- Real Veo previz once the Cloud Run gateway `/video` endpoint is redeployed.
- Orphaned components after Block 0 (VideoEditor.jsx, CutEditor.jsx, MusicPicker) — re-evaluate for reuse in the new render/revise flow.
